{
  "name": "@kinarad/service",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "common/*",
    "infrastructure/*",
    "modules/*"
  ],
  "scripts": {
    "tf:init": "terraform init",
    "build": "lerna exec yarn build",
    "test": "lerna exec yarn test",
    "deploy:terraform": "terraform apply -auto-approve",
    "deploy": "run-s build deploy:terraform"
  },
  "devDependencies": {
    "lerna": "^3.22.1",
    "npm-run-all": "^4.1.5"
  }
}
